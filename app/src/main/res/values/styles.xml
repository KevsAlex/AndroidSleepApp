<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Customize your theme here. -->
    <style name="MyToolbarTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorPrimary">@android:color/white
        </item> <!-- text color of Toolbar Style -->
        <item name="android:textColorSecondary">@android:color/white
        </item> <!-- Color of Overflow Menu (3 Dots Icon)-->
    </style>

    <style name="MyPopupTheme" parent="ThemeOverlay.AppCompat.Light">
        <!-- android:background Este atributo es para el color de fondo del menu 3 puntos-->
        <!-- item name="android:background">@color/overflowMenuBg</item -->

        <!-- android:textColorPrimary Color de fondo de la letra del menu-->
        <item name="android:textColorPrimary">@color/colorNegro</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
